kind: pipeline
name: some-checks

steps:
- name: build image
  image: plugins/docker
  settings:
    repo: local/build
    registry: registry:5000

- name: rst-lint
  image: local/build
  commands:
  - make lint-rst

trigger:
  branch:
  - master
  event:
  - push
